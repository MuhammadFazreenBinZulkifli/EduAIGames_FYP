# Frontend environment (copy to .env.local for local overrides)
#
# Leave empty when the API is on the same domain (recommended for production):
#   https://yourdomain.com serves both the React app and /api/*
#
# Set only when the API lives on a different host:
# VITE_API_URL=https://api.yourdomain.com

VITE_API_URL=
